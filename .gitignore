# Ignore build outputs from performing a nix-build or `nix build` command
result
result-*

# Ignore automatically generated direnv output
.direnv

# nixos wsl tar
nixos-wsl.tar.gz

# OS trash
.DS_Store
.DS_Store?

#
# Claude ignores
#
# Tickets
.scratch/
# Personal preferences and auto-allow permissions
.claude/settings.local.json
# Process lock for background schedules
.claude/scheduled_tasks.lock
# Parallel agent workspaces (duplicate codebase clones)
.claude/worktrees/
# Auto-memory state and local conversation history
.claude/memory/
.claude/plans/
# Local, per-developer project instructions
CLAUDE.local.md
