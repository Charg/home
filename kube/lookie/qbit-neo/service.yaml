---
apiVersion: v1
kind: Service
metadata:
  name: qbit-neo
  namespace: default
  labels:
    app.kubernetes.io/instance: qbit-neo
    app.kubernetes.io/name: qbit-neo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: qbit-neo
    app.kubernetes.io/name: qbit-neo
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
