---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: accord-migrations
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: ghcr.io/charg/charts
    chart: accord
    targetRevision: 0.21.2
    helm:
      valuesObject:
        image:
          tag: 0.21.2
        imagePullSecrets:
          - name: ghcr-pull-creds
        existingSecret: accord-env
        web:
          enabled: false
        worker:
          enabled: false
        registerSchedules:
          enabled: false
        migrations:
          enabled: true
          annotations:
            argocd.argoproj.io/hook: Sync
            argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
  destination:
    namespace: ac
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
