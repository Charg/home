---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: temporal-postgres
spec:
  retentionPolicy: "3d"
  configuration:
    destinationPath: "s3://backups/temporal-postgres"
    endpointURL: "http://backup-rustfs.backup.svc.cluster.local:9000"
    s3Credentials:
      accessKeyId:
        name: backup-rustfs-credentials
        key: access-key
      secretAccessKey:
        name: backup-rustfs-credentials
        key: secret-key
    wal:
      compression: gzip
    data:
      compression: gzip
