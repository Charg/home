---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: temporal-postgres-daily
spec:
  schedule: "0 30 2 * * *"
  backupOwnerReference: cluster
  cluster:
    name: temporal-postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
